name: One-Time Push 

on:
 push:
  branches:
   - master

jobs:
  push-to-nest-next-copilot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Unzip file
        run: |
          git reset 822ad51 --hard 
          git commit -m"reset"
          git push origin master -f
